<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LIGHT theme: clean white + gold accent -->
    <style name="Theme.AbayBids" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/brand_primary_dark</item>
        <item name="colorAccent">@color/brand_accent</item>
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@color/brand_primary_dark</item>
        <item name="android:navigationBarColor">@color/bg</item>
        <item name="colorOnPrimary">@android:color/white</item>
    </style>

    <!-- Native Android 12+ splash screen: brand-navy background + logo,
         handed off to the real theme the instant MainActivity is ready
         (see MainActivity.installSplashScreen()). -->
    <style name="Theme.AbayBids.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_primary_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.AbayBids</item>
    </style>
</resources>
