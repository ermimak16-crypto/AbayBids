<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".AbayBidsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbayBids"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- ===== Launcher: native dashboard ===== -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AbayBids.Starting"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== Tender list (search/filter) ===== -->
        <activity
            android:name=".ui.TenderListActivity"
            android:exported="false"
            android:label="@string/title_tenders"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Tender detail — deep-link target ===== -->
        <activity
            android:name=".ui.TenderDetailActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="abaybids" android:host="tender" />
            </intent-filter>
        </activity>

        <!-- ===== Add / edit tender ===== -->
        <activity
            android:name=".ui.AddTenderActivity"
            android:exported="false"
            android:label="@string/title_add_tender"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Contracts (native: add/edit/delete + archived by year) ===== -->
        <activity
            android:name=".ui.ContractsActivity"
            android:exported="false"
            android:label="Contracts"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Settings ===== -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/title_settings"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Calendar: month grid of tender deadlines ===== -->
        <activity
            android:name=".ui.CalendarActivity"
            android:exported="false"
            android:label="Calendar"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Reports: computed stats + CSV/PDF export ===== -->
        <activity
            android:name=".ui.ReportsActivity"
            android:exported="false"
            android:label="Reports"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Clients: aggregated per-customer ===== -->
        <activity
            android:name=".ui.ClientsActivity"
            android:exported="false"
            android:label="Clients &amp; Organizations"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Analytics: charts computed from DB ===== -->
        <activity
            android:name=".ui.AnalyticsActivity"
            android:exported="false"
            android:label="Analytics"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Bid History: status-change audit log ===== -->
        <activity
            android:name=".ui.BidHistoryActivity"
            android:exported="false"
            android:label="Bid History"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Documents: attach / open / delete / retry uploads ===== -->
        <activity
            android:name=".ui.DocumentsActivity"
            android:exported="false"
            android:label="Documents"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== Products: aggregated per-category ===== -->
        <activity
            android:name=".ui.ProductsActivity"
            android:exported="false"
            android:label="Products &amp; Services"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===== FileProvider for native document opening / sharing ===== -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ===== Broadcast receiver that posts the reminder ===== -->
        <receiver
            android:name=".notification.TenderAlarmReceiver"
            android:exported="false" />

        <!-- ===== Reschedule all alarms after device reboot ===== -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ===== FCM messaging service ===== -->
        <service
            android:name=".notification.AbayMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
